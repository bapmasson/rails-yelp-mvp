<div class="container">
  <div class="mt-3 text-center border border-light rounded">
    <h1><%= @restaurant.name %></h1>
    <h2 class="fw-light"><%= @restaurant.category.capitalize %> cuisine</h2>
    <p class="fst-italic fw-light">
      <%= @restaurant.address %>
      <%= @restaurant.phone_number %>
    </p>
  </div>

</div>
